import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#484848",
    },
    secondary: {
      main: "#008c8c",
    },
    primaryBtn: {
      main: "#4a5568",
    },
  },
});

export default theme;
